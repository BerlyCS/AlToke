{
  "name": "backend",
  "version": "1.0.50",
  "type": "module",
  "module": "src/index.js",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "test": "bun test",
    "type-check": "tsc --noEmit",
    "lint": "bun run lint:fix",
    "lint:check": "oxlint .",
    "lint:fix": "oxlint . --fix",
    "format": "oxfmt .",
    "format:check": "oxfmt . --check"
  },
  "dependencies": {
    "elysia": "latest"
  },
  "devDependencies": {
    "bun-types": "latest",
    "oxfmt": "^0.45.0",
    "oxlint": "~1.60.0",
    "typescript": "~6.0.0"
  }
}
