name: CD

on:
  workflow_run:
    workflows:
      - CI
    types:
      - completed
  workflow_dispatch:

permissions:
  contents: read

jobs:
  resolve-target:
    name: Resolve deployment target
    runs-on: ubuntu-latest
    if: |
      (github.event_name == 'workflow_run' && github.event.workflow_run.conclusion == 'success') ||
      github.event_name == 'workflow_dispatch'
    outputs:
      should_deploy: ${{ steps.resolve.outputs.should_deploy }}
      deploy_branch: ${{ steps.resolve.outputs.deploy_branch }}
      deploy_path: ${{ steps.resolve.outputs.deploy_path }}
      environment_name: ${{ steps.resolve.outputs.environment_name }}
    steps:
      - name: Select branch deployment target
        id: resolve
        shell: bash
        env:
          HEAD_BRANCH: ${{ github.event_name == 'workflow_run' && github.event.workflow_run.head_branch || github.ref_name }}
          DEVOPS_DEPLOY_PATH: ${{ secrets.DEVOPS_DEPLOY_PATH }}
          STAGING_DEPLOY_PATH: ${{ secrets.STAGING_DEPLOY_PATH }}
          PROD_DEPLOY_PATH: ${{ secrets.PROD_DEPLOY_PATH }}
        run: |
          set -euo pipefail

          should_deploy=false
          deploy_path=""
          environment_name=""

          case "$HEAD_BRANCH" in
            feature/devops)
              should_deploy=true
              deploy_path="$DEVOPS_DEPLOY_PATH"
              environment_name="devops"
              ;;
            dev)
              should_deploy=true
              deploy_path="$STAGING_DEPLOY_PATH"
              environment_name="staging"
              ;;
            main)
              should_deploy=true
              deploy_path="$PROD_DEPLOY_PATH"
              environment_name="production"
              ;;
          esac

          if [[ "$should_deploy" == "true" && -z "$deploy_path" ]]; then
            echo "Deployment path secret is empty for branch $HEAD_BRANCH" >&2
            exit 1
          fi

          echo "should_deploy=$should_deploy" >> "$GITHUB_OUTPUT"
          echo "deploy_branch=$HEAD_BRANCH" >> "$GITHUB_OUTPUT"
          echo "deploy_path=$deploy_path" >> "$GITHUB_OUTPUT"
          echo "environment_name=$environment_name" >> "$GITHUB_OUTPUT"

  deploy:
    name: Deploy via SSH
    runs-on: ubuntu-latest
    needs: resolve-target
    if: |
      (github.event_name != 'workflow_run' || github.event.workflow_run.conclusion == 'success') &&
      needs.resolve-target.outputs.should_deploy == 'true'
    environment: ${{ needs.resolve-target.outputs.environment_name }}
    concurrency:
      group: cd-${{ needs.resolve-target.outputs.environment_name }}
      cancel-in-progress: false
    steps:
      - name: Run remote deployment commands
        uses: appleboy/ssh-action@v1.2.2
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          command_timeout: 30m
          envs: DEPLOY_PATH
          request_pty: false
          script: |
            bash -lc '
              set -euo pipefail

              deploy_path="$DEPLOY_PATH"

              if [[ ! -d "$deploy_path" ]]; then
                echo "Deploy path not found: $deploy_path" >&2
                exit 1
              fi

              cd "$deploy_path"
              docker compose pull
              docker compose up -d
              docker compose ps
            '
        env:
          DEPLOY_PATH: ${{ needs.resolve-target.outputs.deploy_path }}
